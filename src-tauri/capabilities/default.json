{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for vs2lma",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:tray:default",
    "shell:allow-spawn",
    "shell:allow-execute",
    "shell:allow-kill",
    "notification:default"
  ]
}
